[pytest]
testpaths = .
python_files = test_*.py
python_functions = test_*
markers =
    slow: 慢速测试（延迟响应等），默认跳过
    smoke: 冒烟测试，核心功能快速验证
    auth: 认证相关测试
    data_format: 数据格式测试
    edge_case: 边界/异常场景测试
    performance: 性能相关测试
addopts =
    -v
    --tb=short
    --strict-markers
